function reversedCharacters(character1, character2, character3) {
    console.log(`${character3} ${character2} ${character1}`);
}
reversedCharacters();
// Write a program that takes 3 parameters (characters) and prints them in reversed order with a space between them.